import * as THREE from "three";
// model1 name:"Plane050__0"
// model2 name:"lambert1"
const state = {
  activeMesh: {},
  activeMeshName: "Plane050__0",
  cameraPos: new THREE.Vector3(6, 2, 4),
  target: new THREE.Vector3(4, 0, 0),
  shouldUpdate: true,
};

export default state;
